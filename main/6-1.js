function over(obj) // function over(obj): 함수 작성입니다. obj는 전달받은 div 태그를 가리킴
{
  obj.style.background="yellow"; // div태그 배경 스타일을 노란색으로 합니다.
}
function out(obj) // function out(obj): 함수 작성입니다.
{
  obj.style.background="white"; // div태그 배경 스타일을 흰색으로 합니다.
}
